const NAME = "GOOGLE_OPTIMIZE";
const CNameMapping = {
  [NAME]: NAME,
  "Google Optimize": NAME,
  GoogleOptimize: NAME,
  Googleoptimize: NAME,
  GOOGLEOPTIMIZE: NAME,
};

export { NAME, CNameMapping };
