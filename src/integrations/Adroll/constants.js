const NAME = 'ADROLL';
const CNameMapping = {
  [NAME]: NAME,
  Adroll: NAME,
  'Ad roll': NAME,
  'ad roll': NAME,
  adroll: NAME,
};
const requiredConfigsList = ['advId'];

export { NAME, CNameMapping, requiredConfigsList };
